[project]
name = "trafficsimulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mohsen khodaparast", email = "mohsen.developer11@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "osmium>=4.3.1",
]

[project.scripts]
trafficsimulation = "trafficsimulation:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
